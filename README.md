# cat-cafe
Educational project

1. Create a `.env` file in the project root.

2. Add PostgreSQL credentials:

   CAT_SERVICE_DB_NAME=your_database_name
   CAT_SERVICE_DB_USER=your_database_user
   CAT_SERVICE_DB_PASSWORD=your_database_password

3. Start the project:

   docker compose up --build